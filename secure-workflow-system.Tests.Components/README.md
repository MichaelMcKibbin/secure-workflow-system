// Component tests can be added using BUnit once the service injection patterns are established.
// BUnit supports testing Blazor components with proper setup of services and cascading parameters.
// For now, integration tests provide database-level validation and unit tests cover business logic.
